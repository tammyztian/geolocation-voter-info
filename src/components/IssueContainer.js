'use strict';

import React from 'react';

const IssueContainer = (props) => {

    // const issues = props.contests.map(contests )



    return(
    <div className= "app informationBox">
        {console.log(`I'm calling from issuecontainer ${props.contests}`)}
    </div>
    );
}
export default IssueContainer